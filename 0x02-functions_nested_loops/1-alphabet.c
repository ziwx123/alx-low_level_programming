#include "main.h"
/**
 * main - Entry Point
 * Description: Prints the alphabet in lowercase using a function
 * Return: Always 0 (success)
 */
int mai(void)
{
	char i = 'a';
	for (i = 'a'; i <= 'z'; i++)
	{
	  print_alphabet(i);
	}
	return (0);

}
